services:
  picopark-server:
    build:
      context: ./host
      dockerfile: Dockerfile
    container_name: picopark-server
    ports:
      - "3000:3000"
      - "5173:5173"
    restart: unless-stopped
